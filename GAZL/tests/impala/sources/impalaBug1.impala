const int FALSE = 0;
const int TRUE = 1;

function fTSTBIT(int mask, int bit)  // FIX : replace with inline
returns int result
locals int i
{
	result;
	result = FALSE;
	i = ((((1+bit)+mask )));
	if (i != 0) result = TRUE;
	assert((((1+bit)+mask ) != 0));
	if (1+bit+mask != 0) result = TRUE;
	if (((1+(bit+mask) != 0))) result = TRUE;
	if ((1+bit)+mask != 0) result = TRUE;
}

